FROM resin/%%RESIN_MACHINE_NAME%%-python

#RUN apt-get update && apt-get install -yq \
#    git \
#    nano \
#    curl

RUN pip install RPi.GPIO

WORKDIR /usr/src/app
COPY . ./
RUN apt-get clean && rm -rf /var/lib/apt/lists/*
CMD ["bash","/usr/src/app/main.py"]
